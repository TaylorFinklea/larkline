-- Atlassian: Active Sprint — placeholder until Phase D.
lark.register({
    on_run = function()
        return {
            title = "Active Sprint",
            items = {
                { label = "Coming in v0.12.0 Phase D", detail = "Active-sprint issue list via the Jira Agile API", icon = "🚧" },
            },
        }
    end,
})
