-- Atlassian: New Jira Issue — placeholder until Phase D.
lark.register({
    on_run = function()
        return {
            title = "New Jira Issue",
            items = {
                { label = "Coming in v0.12.0 Phase D", detail = "Create-issue form", icon = "🚧" },
            },
        }
    end,
})
