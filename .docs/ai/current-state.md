# Current State

> Updated: 2026-04-18

## Active Branch

`main`

## Recent Progress (this session)

- **v0.10.0 complete:** Distribution + lark.nvim v2 — 8 phases (A–H)
  - Phase A: Tracing routed to `$XDG_STATE_HOME/larkline/lark.log` via `tracing-appender` rolling daily + non-blocking writer. TUI never leaked logs again.
  - Phase B: `lark plugin sync` now repairs dead symlinks and relinks stale cache paths. `--force` overwrites user-modified plugins after an interactive y/N prompt (non-TTY auto-skips).
  - Phase C: crates.io publish wiring. `Cargo.toml` `exclude` list trims the tarball to 37 files / 595 KiB. `publish-crate` job in release.yml gated on stable semver tags, backed by `CRATES_IO_TOKEN`, `continue-on-error` with auto-issue on failure. PR CI runs `cargo publish --dry-run`.
  - Phase D: README install docs for Homebrew, Cargo, AUR, Nix, GitHub Releases. CHANGELOG draft rolls up v0.6 → v0.10.
  - Phase E: `packaging/aur/larkline-bin/PKGBUILD` + publish workflow docs.
  - Phase F: `flake.nix` (package + app + devshell). `flake.lock` to be generated by a maintainer with nix installed.
  - Phase G: `ActionKind::NvimEdit` plus a new Lua host function for sending ex commands. Both use `nvim --server $NVIM --remote-send` under the hood with key-notation escaping; fall back to `open_url` outside nvim. `file-search` and `notes` plugins surface "Open in Neovim" actions when `$NVIM` is set. `lark.nvim/README.md` documents the handshake.
  - Phase H: `scripts/release.sh` gained a `set <version>` mode to jump 0.5.0 → 0.10.0 directly.

- **v0.9.0 (earlier this session):** Internal Quality — `app.rs` 4027 → 2851 lines across 8 focused modules. Prefetch concurrency cap (8). Slow-plugin `info!` profiling. Widget auto-refresh skipped when dashboard hidden.

## Current Version

`Cargo.toml` still on `0.5.0` — the release cut to `0.10.0` is Phase H's responsibility and is explicitly gated: the plan is not to push the tag until Taylor green-lights. Running `bash scripts/release.sh set 0.10.0` will bump Cargo.toml + Formula, commit, tag, and push — triggering the full CI pipeline (binaries, GitHub Release, Homebrew tap bump, crates.io publish).

Accumulated unreleased work: v0.6.0, v0.7.0, v0.8.0, v0.9.0, v0.10.0.

## Validation

- `cargo test` — all suites green; new tests this session include log path resolution (3), sync classifier (5), and nvim action serde (2).
- `cargo clippy -- -D warnings` — clean.
- `cargo fmt -- --check` — clean.
- `cargo publish --dry-run --locked` — 37 files / 595 KiB uncompressed / 133 KiB compressed.

## New Files This Session

| File | Purpose |
|------|---------|
| `src/widgets.rs` | Widget state helpers (v0.9.0-A) |
| `src/power_menu.rs` | Power menu builder (v0.9.0-B) |
| `src/plugin_manager_state.rs` | Plugin Manager state builder (v0.9.0-C) |
| `src/app_output.rs` | Output + form helpers (v0.9.0-D) |
| `src/widget_actions.rs` | 12 widget action handlers (v0.9.0-E) |
| `src/form_actions.rs` | 11 form action handlers (v0.9.0-E) |
| `src/plugin_manager_actions.rs` | 6 plugin manager action handlers (v0.9.0-E) |
| `flake.nix` | Nix flake (v0.10.0-F) |
| `packaging/aur/larkline-bin/PKGBUILD` | AUR binary package (v0.10.0-E) |
| `packaging/aur/README.md` | AUR publish workflow docs (v0.10.0-E) |
| `lark.nvim/README.md` | lark.nvim documentation (v0.10.0-G) |

## Pending Before Tag

1. Add `CRATES_IO_TOKEN` as a repo secret on GitHub — without it the `publish-crate` job will fail on the first tag (gracefully — the failure posts an issue, doesn't block the release).
2. Generate `flake.lock` on a machine with `nix` installed and commit it. Not blocking — CI regenerates on first build.
3. Run `bash scripts/release.sh set 0.10.0` when ready to ship.
